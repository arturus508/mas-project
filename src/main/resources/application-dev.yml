spring:
  datasource:
    url: jdbc:mysql://localhost:3307/healthfitness?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: example_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update   
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: false      

  sql:
    init:
      mode: never      